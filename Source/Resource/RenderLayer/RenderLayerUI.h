#pragma once
#include "RenderLayer.h"

namespace Origin {

class RenderLayerUI : public RenderLayer {

public:
    RenderLayerUI(Object* parent = nullptr);
    ~RenderLayerUI();

private:

};

} // Origin
